{
  "name": "wendy-rich-menu",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix liff",
    "build": "npm run build --prefix liff && npm run build --prefix server",
    "verify-deploy": "node scripts/verify-deploy.js",
    "dev:server": "npm run dev --prefix server",
    "dev:liff": "npm run dev --prefix liff",
    "create-rich-menu": "npm run create-rich-menu --prefix server",
    "confirm-purchase": "npm run confirm-purchase --prefix server",
    "list-status": "npm run list-status --prefix server",
    "list-purchases": "npm run list-purchases --prefix server",
    "reset-member": "npm run reset-member --prefix server"
  }
}
